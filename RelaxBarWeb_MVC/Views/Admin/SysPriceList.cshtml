@model List<RelexBarDLL.SysPrice>
@for (int i = 0; i < Model.Count; i++)
{
    <tr>
        <td>@(Model[i].InOut==1?"收入":"支出")</td>
        <td>@Model[i].Money</td>
        <td>@Model[i].Remark</td>
        <td>@Model[i].CreateTime</td>
    </tr>
}
<script>createFooter(@ViewData["sum"])</script>
